import React from "react";
import { MainContainer, HeaderSection, BodySection, Title } from "./style";

const Main = () => {
  return (
    <MainContainer>
      <HeaderSection>
        <Title>Dashboard</Title>
      </HeaderSection>
      <BodySection></BodySection>
    </MainContainer>
  );
};

export default Main;
